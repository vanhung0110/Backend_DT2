package com.example.hungdt2.sms;

/**
 * DevSmsSender removed from runtime use. Kept only as a deprecated placeholder to avoid compile errors
 * in older tests or code that might reference the type. It is intentionally NOT a Spring component.
 */
@Deprecated
public final class DevSmsSender {
    private DevSmsSender() { /* no-op */ }
}
